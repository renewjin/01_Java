package com.kh.collectionEx;
/*
 Map
 	키(key) - 값(value) 한 쌍의 집합을 이루는 구조
 	
 	키는 숫자를 넣어도 되고, 문자를 넣어도 됨, 문자열을 넣어도 되고 
 		어떤떤 값이 들어가도 괜찮음
 	값 또한 모든 값이 들어 갈 수 있음
 	
 	값은 중복이 가능하지만 키는 중복이 될 수 없음
 	만약 중복된 키가 존재한다면 이전 키에 들어 있는 값은 나중에 작성한 키의 값으로 대체됨
 	순서가 보장되지 않음 만약 순서를 보장해야하는 경우 LinkedHashMap을 사용할 수 있음
 	
 	HashMap 가장 많이 사용 (빠른 검색속도)
 	TreeMap 이진트리로 빠른 검색이 필요할 때 사용 (정렬된 데이터를 필요할 경우)
 	LinkedMap 순서를 보장하는 연결이 필요할 때 사용 (순서를 유지하면서 저장하기 때문에 순서가 중요)
 	
 	메서드
 		put(key, value) : 지정된 키와 값을 맵에 추가
 							만약 동일한 키가 이미 존재한다면 이전값은 새로운 값으로 대체
 		get(key) : 지정된 키에 해당하는 값을 보여줌
 					만약 키가 존재하지 않는다면 null 반환
 		containsKey(key) : 맵에 지정된 키가 포함되어 있는지 확인
 		containsValue(value) : 맵에 지정된 값이 포함되어 있는지 확인
 		remove(key) : 지정된 키와 해당하는 값을 제거
 		keySet() : 키를 포함하는 집합을 반환
 		values() : 모든 값들을 포함하는 컬렉션 반환
 		entrySet() : 모든 키-쌍을 포함하는 집합을 반환
		size() : key-value 쌍의 개수 반환(저장되어있는)
 		isEmpty() : 비어있는지 확인
		clear() : 모두 제거
 		
 */
public class Map {

}
